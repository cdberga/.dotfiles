# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $win Mod4
set $alt Mod1

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:monospace 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:Courier New 10

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$win to drag floating windows to their wanted position
floating_modifier $win

### Init Read for Desktop Image ###
# start a terminal
bindsym $win+Return exec xterm
bindsym $win+Ctrl+t exec i3-sensible-terminal
# start dmenu (a program launcher)
bindsym $win+d exec dmenu_run -i -l 20
### End Read for Desktop Image ###

# kill focused window
bindsym $win+q kill


# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $win+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $win+z focus left
bindsym $win+x focus right
#bindsym $win+ccedilla focus right

# alternatively, you can use the cursor keys:
bindsym $win+Left focus left
bindsym $win+h focus left
bindsym $win+Down focus down
bindsym $win+j focus down
bindsym $win+Up focus up
bindsym $win+k focus up
bindsym $win+Right focus right
bindsym $win+l focus right

# move focused window
bindsym $win+Shift+h move left
bindsym $win+Shift+j move down
bindsym $win+Shift+k move up
bindsym $win+Shift+l move right
#bindsym $win+Shift+ccedilla move right

# alternatively, you can use the cursor keys:
bindsym $win+Shift+Left move left
bindsym $win+Shift+Down move down
bindsym $win+Shift+Up move up
bindsym $win+Shift+Right move right

# Go to previous or next workspace
bindsym $win+Ctrl+Left workspace prev
bindsym $win+Ctrl+Right workspace next

# Move to previous or next workspace
bindsym $win+Ctrl+Shift+Left move container to workspace prev; workspace prev
bindsym $win+Ctrl+Shift+Right move container to workspace next; workspace next

# split toggle
bindsym $win+Ctrl+semicolon split toggle

# split in horizontal orientation
#bindsym $win+h split h
# split in vertical orientation
#bindsym $win+v split v

# enter fullscreen mode for the focused container
bindsym $win+Ctrl+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $win+semicolon layout toggle
#bindsym $win+s layout stacking
#bindsym $win+Ctrl+w layout tabbed
#bindsym $win+e layout toggle split

# toggle tiling / floating
bindsym $win+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $win+space focus mode_tilling

# focus the parent container
bindsym $win+a focus parent

# focus the child container
#bindsym $win+d focus child

# switch to workspace
bindsym $win+1 workspace 1
bindsym $win+2 workspace 2
bindsym $win+3 workspace 3
bindsym $win+4 workspace 4
bindsym $win+5 workspace 5
bindsym $win+6 workspace 6
bindsym $win+7 workspace 7
bindsym $win+8 workspace 8
bindsym $win+9 workspace 9
bindsym $win+0 workspace 10

# move focused container to workspace
bindsym $win+Shift+1 move container to workspace 1
bindsym $win+Shift+2 move container to workspace 2
bindsym $win+Shift+3 move container to workspace 3
bindsym $win+Shift+4 move container to workspace 4
bindsym $win+Shift+5 move container to workspace 5
bindsym $win+Shift+6 move container to workspace 6
bindsym $win+Shift+7 move container to workspace 7
bindsym $win+Shift+8 move container to workspace 8
bindsym $win+Shift+9 move container to workspace 9
bindsym $win+Shift+0 move container to workspace 10

### Init Read for Desktop Image ###
# Text Editor
bindsym $win+w exec "xed"
# Calculator (Gnome)
bindsym $win+c exec gnome-calculator
# Chrome
bindsym $win+g exec "google-chrome"
# SQLite
bindsym $win+Ctrl+l exec "$HOME/DevEnv/SQLiteStudio/sqlitestudio"
# Brave
bindsym $win+b exec "firejail brave-browser"
# Lock Screen
bindsym $win++ccedilla exec "i3lock"
# Shutdown
bindsym $win+Ctrl+x exec "/sbin/shutdown now"
# Restart
bindsym $win+Ctrl+z exec "/sbin/shutdown -r now"

bindsym $win+f exec "firejail firefox"

bindsym $win+F1 exec "notify-send `firejail --list`"

# Add bookmark
bindsym $win+Ctrl+b exec "$HOME/work/scripts/bookmarks.sh add"

# Launch bookmark
bindsym $win+Ctrl+o exec "$HOME/work/scripts/bookmarks.sh open"

# Delete bookmark
bindsym $win+Ctrl+d exec "$HOME/work/scripts/bookmarks.sh delete"

# IR Rendavar 
bindsym $win+Ctrl+i exec "$HOME/work/scripts/ir-rendavar.sh"

# Edit bashrc file
bindsym $win+Shift+b exec "xterm -e mcedit $HOME/.bashrc
# Edit i3 config file
bindsym $win+Shift+i exec "xterm -e nvim $HOME/.config/i3/config"
# Open scripts project folder in xterm
#bindsym $win+s exec "xterm -cd $HOME/work/scripts"

# Flameshot
bindsym Print exec "flameshot gui"
# Gif Record
bindsym Ctrl+Print exec "$HOME/work/scripts/record-screen.sh"
# Gif Recording Stop
bindsym Ctrl+Shift+Print exec "killall ffmpeg"
# user logout (Kill session)
bindsym $win+Ctrl+k exec "pkill -KILL -u $USER"
# user change
bindsym $win+Shift+u exec "dm-tool switch-to-greeter"
# Libreoffice
bindsym $win+o exec "libreoffice"
# Authy
bindsym $win+Ctrl+a exec "authy"
# McEdit clip to clipboard
bindsym $win+Shift+m exec "find ~/.local/share/mc/mcedit -name mcedit.clip | xclip -i -sel copy ~/.local/share/mc/mcedit/mcedit.clip &"
# Open general menu
bindsym $win+m exec "$HOME/work/scripts/menus_new.sh -a $HOME/work/scripts/menus.cfg -t 'Menu: '"
# Pass menu login
bindsym $win+Ctrl+p exec "$HOME/work/scripts/passmenu.sh -m d"
# Pass menu user
bindsym $win+u exec "$HOME/work/scripts/passmenu.sh -m d -f user"
# Pass menu site
bindsym $win+Shift+p exec "$HOME/work/scripts/passmenu.sh -m d -f site"
# Pass menu pwd
bindsym $win+p exec "$HOME/work/scripts/passmenu.sh -m d -f pwd"
# Totp with dmenu
bindsym $win+t exec "$HOME/work/scripts/totp.sh -tm"
# Profit
bindsym $win+$alt+p exec "env WINEPREFIX='/home/tiwork/.wine' wine-stable C:\\\\users\\\\tiwork\\\\Application\\ Data\\\\Nelogica\\\\Profit\\\\profitchart.exe"

### End Read for Desktop Image ###

# pavucontrol 4 sound
bindsym $win+Ctrl+F12 exec pavucontrol
# Bluetooth Control
bindsym $win+Ctrl+F11 exec blueberry
bindsym XF86AudioMute exec "amixer -D pulse set Master toggle"
bindsym XF86AudioRaiseVolume exec "amixer -c 0 -q set Master 2dB+"
bindsym XF86AudioLowerVolume exec "amixer -c 0 -q set Master 2dB-"

### Work with VMs
bindsym $win+Ctrl+Shift+f exec "vboxmanage startvm umin-f2"
bindsym $win+Ctrl+Shift+c exec "vboxmanage startvm umin-cef"
bindsym $win+Ctrl+Shift+p exec "$HOME/work/scripts/qemu-debian-start.sh"
# open Oracle Virtualbox
bindsym $win+Ctrl+m exec "virtualbox"
# open virt-manager
bindsym $win+Ctrl+v exec "virt-manager"

### Init Read for Desktop Image ###
# config file reload
bindsym $win+Shift+c "reload"
# i3 update and restart # inplace (preserves your layout/session, can be used to upgrade i3)
#bindsym $win+Shift+r "restart"
# i3 exit #(logs you out of your X session)
bindsym $win+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"
### End Read for Desktop Image ###

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym ccedilla resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px
        bindsym Down resize grow height 10 px
        bindsym Up resize shrink height 10 px
        bindsym Right resize grow width 10 px

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $win+r mode "resize"

exec --no-startup-id nm-applet

workspace_layout tabbed

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
#        status_command i3status #-c ~/.config/i3/i3status.conf
        status_command i3blocks
        #separator_symbol ⁞
        separator_symbol |

#        tztime local {
#                format = "%b - %Y.%m.%d %H:%M"
#                hide_if_equals_localtime = true
#        }
}
